# Changelog

This fork's history is tracked in `git log` (each commit is one deliberate
step: vendor + patch, trim, rewrite off Next.js, rename). No separate
changelog is kept — the commit messages are the changelog.

Upstream [9router](https://github.com/decolua/9router)'s own changelog, up to
the point this was forked (v0.5.81), is available in its repo history if you
need it; it doesn't describe anything in this fork.
